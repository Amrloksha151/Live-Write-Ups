---
export interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<span
  class="inline-block px-2.5 py-1 bg-slate text-space text-xs font-dm font-semibold rounded-full hover:bg-crimson hover:text-snow transition-colors"
>
  {tag}
</span>
